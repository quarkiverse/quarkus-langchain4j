<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.quarkiverse.langchain4j</groupId>
        <artifactId>quarkus-langchain4j-gpu-llama3-parent</artifactId>
        <version>999-SNAPSHOT</version>
    </parent>

    <artifactId>quarkus-langchain4j-gpu-llama3</artifactId>
    <name>Quarkus LangChain4j - GPULlama3 - Runtime</name>

    <dependencies>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkiverse.langchain4j</groupId>
            <artifactId>quarkus-langchain4j-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5-internal</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.github.beehive-lab</groupId>
            <artifactId>gpu-llama3</artifactId>
            <version>${gpu-llama3.version}</version>
        </dependency>

    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-extension-maven-plugin</artifactId>
                <version>${quarkus.version}</version>
                <executions>
                    <execution>
                        <phase>compile</phase>
                        <goals>
                            <goal>extension-descriptor</goal>
                        </goals>
                        <configuration>
                            <deployment>${project.groupId}:${project.artifactId}-deployment:${project.version}</deployment>
                        </configuration>
                    </execution>
                </executions>
              <configuration>
                <parentFirstArtifacts>
                  <parentFirstArtifact>org.graalvm:graal-sdk</parentFirstArtifact>
                  <parentFirstArtifact>io.github.beehive-lab:gpu-llama3</parentFirstArtifact>
                </parentFirstArtifacts>
                <runnerParentFirstArtifacts>
                  <runnerParentFirstArtifact>org.graalvm:graal-sdk</runnerParentFirstArtifact>
                  <runnerParentFirstArtifact>io.github.beehive-lab:gpu-llama3</runnerParentFirstArtifact>
                </runnerParentFirstArtifacts>
              </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Java 21: Enable preview features to match gpu-llama3 compilation -->
        <profile>
            <id>jdk21-with-preview</id>
            <activation>
                <jdk>21</jdk>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-compiler-plugin</artifactId>
                        <configuration>
                            <!--
                                Target Java 21 with preview features to match gpu-llama3 dependency
                             -->
                            <source>21</source>
                            <target>21</target>
                            <release combine.self="override" />  <!-- make sure -release is not passed to javac-->
                            <compilerArgs>
                                <arg>--enable-preview</arg>
                                <arg>--add-modules=jdk.incubator.vector</arg>
                            </compilerArgs>
                            <annotationProcessorPaths>
                                <path>
                                    <groupId>io.quarkus</groupId>
                                    <artifactId>quarkus-extension-processor</artifactId>
                                    <version>${quarkus.version}</version>
                                </path>
                            </annotationProcessorPaths>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Java 22-23: Compile to Java 21 target for compatibility -->
        <profile>
            <id>jdk22-23-with-preview</id>
            <activation>
                <jdk>[22,23]</jdk>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-compiler-plugin</artifactId>
                        <configuration>
                            <!--
                                Compile with JDK 22-23 but target Java 21 for compatibility
                             -->
                            <source>21</source>
                            <target>21</target>
                            <release combine.self="override" />  <!-- make sure -release is not passed to javac-->
                            <compilerArgs>
                                <arg>--enable-preview</arg>
                                <arg>--add-modules=jdk.incubator.vector</arg>
                            </compilerArgs>
                            <annotationProcessorPaths>
                                <path>
                                    <groupId>io.quarkus</groupId>
                                    <artifactId>quarkus-extension-processor</artifactId>
                                    <version>${quarkus.version}</version>
                                </path>
                            </annotationProcessorPaths>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Java 24+: Use current JDK's preview features -->
        <profile>
            <id>jdk24-plus</id>
            <activation>
                <jdk>[24,)</jdk>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-compiler-plugin</artifactId>
                        <configuration>
                            <!--
                                Use JDK 24+ where preview features are supported for current version
                             -->
                            <source>24</source>
                            <target>24</target>
                            <release combine.self="override" />  <!-- make sure -release is not passed to javac-->
                            <compilerArgs>
                                <arg>--enable-preview</arg>
                                <arg>--add-modules=jdk.incubator.vector</arg>
                            </compilerArgs>
                            <annotationProcessorPaths>
                                <path>
                                    <groupId>io.quarkus</groupId>
                                    <artifactId>quarkus-extension-processor</artifactId>
                                    <version>${quarkus.version}</version>
                                </path>
                            </annotationProcessorPaths>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
