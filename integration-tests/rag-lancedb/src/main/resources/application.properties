quarkus.langchain4j.embedding-model.provider=dev.langchain4j.model.embedding.onnx.allminilml6v2.AllMiniLmL6V2EmbeddingModel
quarkus.langchain4j.openai.api-key=blabla

# The config-driven (cloud) store is disabled: the integration test provides a local, file-system based
# "dir" LanceDB store via LanceDbTestStoreProducer, so no LanceDB Cloud credentials are needed in CI.
quarkus.langchain4j.lancedb.default-store-enabled=false
